<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> {{title}} - Utn Backend </title>
</head>
<body>
    <h1>Bienvenidos</h1>
    <h3> {{title}} </h3>
    {{#if message}} <div> {{message}} </div>  {{/if}}

    {{{body}}}
</body>
</html>